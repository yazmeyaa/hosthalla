package host_dialog

css host_form() {
	display: flex;
	flex-direction: column;
	gap: 12px;
}

css host_form_field() {
	display: flex;
	flex-direction: column;
	gap: 6px;
}

css host_form_label() {
	font-size: 0.9rem;
	color: var(--muted);
}
